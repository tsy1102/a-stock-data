# V17.0 重构计划(plan)

> 创建: 2026-08-13 · 状态: **执行完成(降级项已全部清零, 第二轮收尾)** — 以下勾选为实际执行结果
> 更新: 2026-08-13 第二轮全量回归 302 passed / 45 deselected, 0 失败

## 背景与目标

1. **目录整理**: 项目根目录零碎(45 条目) → 归类可读: 7 个支撑模块包化进 `core/`、凭据归位 `credentials/`、孤儿文件清理、每目录补 README
2. **v9.6 遗产清理**: `a-stock-data-v9.6/` 历史使命完成, 连同 SKILL.md 与 3 个对比工具删除
3. **代码重构**: 8 阶段(S1-S8)死代码/传输层/市场代码/数据源重复/报告样板/渲染/composite/缓存适配器

## 用户已确认决策

- 目录整理: **激进包化**(7 模块移入 core/)
- v9.6 时代工具: **三个全删**(capture_baseline / auto_fix_pipeline / compare_baseline)
- 版本: **合并 V17.0 单版本**
- 凭据: **新建 credentials/ 目录**
- 重构决策: S4 先探针实测再合并; S5 含钩子机制; __all__ 直接删

## 执行序列

| 阶段 | 内容 | 风险 | 验证 |
|---|---|---|---|
| P0 | v9.6 遗产清理(目录/SKILL.md/3工具/孤儿db/.gitignore/AGENTS.md §4.1/文档引用) | LOW | py_compile 抽查 + grep 零残留(历史注释保留) |
| P1 | 凭据归位 credentials/ | LOW | py_compile 2 文件 |
| P2 | core/ 包化(7 模块 + 全仓 ~150 处 import + __file__ 3 处 + CLI) | HIGH | 全量 py_compile + 全量测试回归(skip_real) |
| P3 | README 体系(4-5 文档 + 根 README 重写) | LOW | 人工核对 |
| P4 | 重构 S1-S8(死代码→传输层→市场代码→数据源→流水线→渲染→composite→缓存) | LOW-MED | 每阶段 py_compile + 相关测试 |
| P5 | 收尾(VERSION=17.0/CHANGELOG/roadmap/session_notes) | LOW | 度量核对 |

## P2 关键风险与对策(包化)

1. `core/__init__.py` 保持**空**——防循环依赖(全仓懒 import 已有保护, 只改前缀不改形式)
2. `__file__` 路径上提 3 处: stock_cache.py:188、zhb_client.py:97/101(否则 @cached 落空库); zhb_client.py:49 双 dirname 保持(包化后反变正确)
3. mock patch 字符串 2 处必改: test_data_tdx.py:338、test_core_routing.py:139 → `core.tdx_client.xxx`(否则测试假绿)
4. CLI 改造: `python -m core.stock_cache` / `-m core.zhb_sync`; clean_cache.py:36、定时任务文档同步
5. 分步不可行: import 面交叉, 必须一次全量改完 + 全量回归

## 度量基线

- 根目录条目: 45 → 目标 ~18
- 删除: ~3000 行(v9.6 + 3 工具 + SKILL.md + S1 死代码 + S7 composite)
- 新增: ~400 行(公共函数/基类钩子/sc_render/README)
- import 统一: `from core import X` 全仓一致

## 执行状态

- [x] P0 v9.6 清理(目录/SKILL.md/3 工具/孤儿 db/.gitignore/AGENTS.md §4.1/文档引用)
- [x] P1 凭据归位(credentials/ + gd_uploader 4 处 + sc_ths + conftest fixture + gitignore)
- [x] P2 core/ 包化(7 模块 + 全仓 33 文件 import + __file__ 3 处 + CLI: -m core.stock_cache/-m core.zhb_sync)
- [x] P2 验证(全量 py_compile 0 失败 + 全量回归 303 passed)
- [x] P3 README 体系(core/README、stock_common/README、tests 更新、3 运行时 README、根 README 树+职责表)
- [x] P4-S1 死代码清理(21 转发+__all__ 255→230+sc_zhb+12 dp 包装+文档; 测试同步)
- [x] P4-S2 传输层统一(_request_with_retry 别名+4 点迁移; _async_quick EM/GEN 分流+别名; 进程间隔参数化降级)
- [x] P4-S3 市场代码统一(em_secid_prefix 新增+7 处替换+92 bug 修复; _market_code 补 9; is_a_stock 下沉; get_threshold 删除)
- [x] P4-S4 getharden 三合一(探针实测→get_ths_hot_raw 唯一入口; val/mak 委托) + cninfo 下沉(keywords 参数)
- [x] P4-S5 报告样板(5 脚本写尾→save_text_report; ST→name_mark; execute_pipeline 降级)
- [x] P4-S6 sc_render(2026-08-13 第二轮: **部分抽取**——实测三脚本渲染逐段对比, 多评委评审团块
  三处逐字相同 12 行×3 → 抽取 sc_render.render_multi_school_scores; 北向宏观/北向持仓/股东户数/
  评分明细与档位**文案异构**(sht 短线风格 vs med/lng 中线长线风格, 单位/符号/结构均不同),
  参数化成本 > 去重收益, 按 DRY 纪律保留本地实现——详见 sc_render.py docstring)
- [x] P4-S7 composite 终结(lng 8 字段经 CanonicalStockData 逐一核对全部覆盖 → _dp_composite 改由
  cdata 构建(兼容 dict 读取面零改动), 删 get_stock_composite + get_stock_composite_async 220 行,
  全仓零残留, routing 14/14)
- [x] P4-S8 缓存适配器(_kpl/_fuyao → @cached; cached_async 占位移除)
- [x] P5 收尾(VERSION=17.0 + CHANGELOG + roadmap + plan 勾选 + 全量回归 302 passed)
- [x] **R1-R5 第二轮(2026-08-13 续, 降级项清零)**: ts 口径统一(5 脚本→基类 report_ts)、进程间隔
  4 函数→2 核心+4 薄包装(行为保留)、S7 composite 220 行删除、execute_batch_pipeline 抬基类
  (med/lng/sht 批量骨架收敛, prefetch/逐只上传钩子)、S6 sc_render 部分抽取(多评委块 3 处收敛);
  全量回归 302 passed / 45 deselected, 0 失败

> **降级记录已全部清零**(2026-08-13 第二轮)。S6 以"部分抽取+异构保留"收尾——
> 实测证据: 北向宏观 sht 单行带符号 vs med 双行无符号、股东户数 sht 表格 vs med 趋势行、
> 评分档位三脚本文案完全不同——强抽公共函数将引入 5-8 个参数, 配置成本>去重收益。
