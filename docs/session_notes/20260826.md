# Session Notes 2026-08-26

### 1. 🔴 82 份报告全量核查（V17.0.8, 用户任务: 完整核查并修复）

- 核查 82 份报告（VAL×2/MAK×2/SHT×72/MED×2/LNG×2），发现 1 严重 + 1 重要 + 3 类展示问题
- **P0 跌停数假数据(22→0)**:
  - 证据链: 东财 push2ex 跌停池 tc=0 + KPL RiseFallAnalysis dt=0(今日真实跌停=0),
    但 SHT 35 份全报 22、MAK 报 2、VAL 报 4
  - 根因: `get_limit_pool_summary` 跌停兜底读 `get_zhb_full_market_snapshot()`, 盘中快照
    恒为 T-1(8/25)——把 8/25 的 22 只跌停误判为今日(实测 603156 快照 -9.99 vs 今日 -3.19)
  - 修复: 权威链 ① push2ex getTopicDTPool tc(新 `_query_dt_pool_tc`, pool 可空但 tc 权威)
    → ② KPL RiseFallAnalysis dt(独立匿名源, 校验日期) → ③ ZHB 仅当快照日期==目标日期
  - MAK B 段恢复 pool 优先(A 段涨跌幅口径兜底), 撤销 V17.0.4 强制覆盖
- **P1 扣非ROE 全 N/A**: `get_roe_trend_series` F10 路径补扣非ROE=加权ROE×(扣非EPS/基本EPS)
  同源推算(F10 无直接扣非ROE 字段; 弃用 fuyao TTM 混入口径——单期加权不可混排)
- **P2 展示/口径**:
  - MAK E 段涨停名单改真实涨停全量(limit_up_all, 原用梯队 _leaders 含替补致"声称3实际1")
  - 名称优先级腾讯实时名>ZHB(消除 霞客环保/哈高科/二纺机 旧名)
  - MED "静态PE"→"动态PE" 标签修正 + LNG PE(动态) 改 canonical pe_dynamic 同源
  - LNG/MED 解禁明细保留一位小数(原 .0f 四舍五入致明细≠总计)
- 回归 269 passed ✓

### 2. 🔬 字段破解 V17.0.9: tdxstat Col[24] = 货币资金（万元）

- 采集 20260826(19 源 461s, push2 主域冷却), 用 F10 资产负债表逐股对照
- 600519 unknown_24=5351879.9万 = 535.188亿 ≡ F10 2026Q1 货币资金(100% 一致)
- 17 只有数据全匹配(14 最新期精确 + 600675/688500 报告期差=2026Q1值)
- 跨日恒定根因=财报季度才更新(静态财务字段); 历史"成交量/总负债/股本"三假设证伪
- 同步: `zhb_client.py` Col[24] 正名 unknown_24→cash_reserve_wan + `_ZHB_PARSE_SCHEMA` 3→4
  (强制重解析旧 _v3.bin 缓存); data_provider/tdx_client 注释; test_data_zhb 断言(45 passed);
  field_dict 7.3 破解 + P0-1 关闭 + 跨源对照表新增货币资金行

### 3. 🐛 批量报告报错修复 V17.0.9b（2026-08-27 早, 用户报告）

- **LNG 688802(沐曦) UnboundLocalError**: `_pe_src` 只在 `_pe>0` 分支赋值, 亏损股
  (pe_ttm=0/pe_dynamic=-699)走 else 未赋值 → 444 行报错。补 `_pe_src="亏损(无正PE)"`。
  仅亏损股触发, 盈利股全部正常(为何只有 1 只报错的原因)
- **SHT 300475 TypeError**: `resolve_datacenter('margin')` 批量预取偶发返回非 list(dict),
  sht `for d in margin` 遍历 dict keys → `d['date']` 报错。双层防御:
  ① `get_margin_trading_async` to_thread 结果非 list 置 [](源头);
  ② sht 消费端 `if margin and isinstance(margin, list)`; ③ block_trade_async 同步防御
- 回归 269 passed ✓

### 4. 📐 ZHB T-1 对撞规则固化 V17.0.10b

- 用户指引: 连续多天采集下, 今日最新 ZHB(T-1) 可与昨日报告对撞
- 规则写入 CRACKING_METHODOLOGY 〇节 + field_verification README 每日流程:
  ZHB 本地包恒为 T-1; 对撞须与 T-1 当日报告严格对齐; 对撞当日报告先验证字段实时性;
  每次对撞先打印 zhb_date + 报告日期; 结论标注双日期
- capture_field_probe.py: meta.json 增写 zhb_data_date + 完成行打印 ZHB 日期

### 5. 🔬 字段破解 V17.0.10: tdxstat Col[33] = 连板数

- 方法: **日期对齐对撞**(T-1 规则)——今日采集 ZHB(8/26) 与 8/27 MAK 涨停天梯对撞
- 铁证: type(Col33) 与天梯连板 **20/20 完全匹配**(000017=5/003040=4/002084=3 全精确)
- 原"涨停类型族 ztlx 待终核"证伪; 当日涨停时 Col[31]/[32]/[33] 三字段一致=连板数;
  非涨停日 type=None/0, count=涨停累计次数(近N日), lianban=历史高位(异动周期计数)
- 初犯: 用 8/27 天梯对撞 8/27 采集 ZHB(未对齐)得 0/20 假阴性 → T-1 规则修正后 20/20
- 同步: field_dict Col[31]/[32]/[33] 对撞补强(Col33 ⚠️→✅)

### 6. ⏭️ 待办

- Col[31] lianban 终极语义: 用 30 日序列验证"近N日最高连板"假设
- 中报(2026H1)全面披露后复查 600675/688500 cash_reserve_wan 翻期
- push2 主域封禁解除后补采对照估值字段