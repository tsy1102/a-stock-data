# ══════════════════════════════════════════════════════════
# A股全栈数据工具 - 开发环境依赖
# 安装: pip install -r requirements-dev.txt
# 注意: 需先安装运行时依赖: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════

# ── 单元测试（V16.3：从 requirements.txt 移入，运行时不再声明）──
pytest>=7.0
pytest-asyncio>=0.21

# ── 静态类型检查 ─────────────────────────────────────────
mypy>=1.0

# ── 代码格式化 ───────────────────────────────────────────
black>=23.0
